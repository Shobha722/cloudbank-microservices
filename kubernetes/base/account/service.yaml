apiVersion: v1
kind: Service

metadata:
  name: account-service
  namespace: cloudbank

spec:
  selector:
    app: account-service

  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP

  type: ClusterIP
