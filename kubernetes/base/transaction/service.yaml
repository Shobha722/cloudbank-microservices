apiVersion: v1
kind: Service

metadata:
  name: transaction-service
  namespace: cloudbank

spec:
  selector:
    app: transaction-service

  ports:
  - port: 5002
    targetPort: 5002
    protocol: TCP

  type: ClusterIP
