apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: cloudbank

spec:
  selector:
    app: notification-service

  ports:
  - port: 5004
    targetPort: 5004
    protocol: TCP

  type: ClusterIP
