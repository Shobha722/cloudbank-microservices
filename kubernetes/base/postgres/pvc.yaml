apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: postgres-pvc
  namespace: cloudbank

spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 10Gi

